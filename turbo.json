{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist"]
        },

        "lint": {},

        "lint:no-fix": {},

        "test": {
            "outputs": ["coverage"]
        }
    }
}
